name: Model Validation

on:
  schedule:
    - cron: '0 2 * * *'  # Daily at 2 AM UTC
  workflow_dispatch:

jobs:
  validate-model:
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v3
    
    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.10'
        cache: 'pip'
    
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt || echo "requirements.txt install failed"
        pip install pytest
    
    - name: Check model artifacts
      continue-on-error: true
      run: |
        pytest tests/test_model_artifacts.py -v || echo "Model artifacts check skipped (no models yet)"
    
    - name: Validate model performance
      continue-on-error: true
      run: |
        pytest tests/test_model.py -v || echo "Model tests skipped"
    
    - name: Notify on failure
      if: failure()
      uses: actions/github-script@v6
      with:
        script: |
          github.rest.issues.createComment({
            issue_number: context.issue.number,
            owner: context.repo.owner,
            repo: context.repo.repo,
            body: '❌ Model validation failed. Check the logs.'
          })
